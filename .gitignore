# Local secrets & runtime state — NEVER commit
config.json
*.flag
proxy.pid
*.pid
*.bak
*.bak-*
env.cmd
env.sh
model-catalog.json
blindfold/certs/
node_modules/
.DS_Store
Thumbs.db
proxy.log
*.log
config.json.*.tmp
temp/
blindfold.pid
blindfold.log
env-codex.cmd
env-codex.sh
# blindfold --capture writes request and response bodies verbatim
captures/

# archify visual-check writes evidence sidecars next to the diagram it checked
docs/diagrams/*.visual-check.*
# admin API token (mode 0600) — never commit
admin.token
blindfold.json
ui-open.html
# launch-file lock, rotated logs, and the tmp files of atomic writes
.launch.lock
*.log.1
*.tmp
